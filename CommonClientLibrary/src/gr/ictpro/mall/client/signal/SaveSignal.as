package gr.ictpro.mall.client.signal
{
	import mx.utils.ObjectProxy;
	
	import org.osflash.signals.Signal;
	
	public class SaveSignal extends Signal
	{
		public function SaveSignal()
		{
			super(Object);
		}
	}
}