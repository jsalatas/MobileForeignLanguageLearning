package gr.ictpro.mall.client.signal
{
	import org.osflash.signals.Signal;
	
	public class AddViewSignal extends Signal
	{
		public function AddViewSignal(...parameters)
		{
			super(parameters);
		}
	}
}